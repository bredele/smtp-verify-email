export const CONFIG = {
  PORT: 25,
  TIMEOUT: 10000,
  HOSTNAME: "example.com",
  FROM_EMAIL: "test@example.com",
};

export const SMTP_CODES = {
  SERVICE_READY: 220,
  OK: 250,
  ERROR_START: 400,
};